package Exam;

public class Test4 {

	public static void main(String[] args) {
//		Person[] pArr = new Person[3];
//		Person[] pArr = new Person[3] pArr[0]= new Person(n,n) pArr[1]= new Person(n,n) pArr[2]= new Person(n,n) ;
//		배열의 요소값을 초기화 해줘야함
		
		//for(int i=0; i<= pArr.lenght; i++) {//배열의 범위보다 반복문이 더 많이 실행되므로  ArrayIndexOutOfBoundsException 오류 발생.
			//System.out.println(pArr[i].getName());//Person의 배열요소인 pArr 객체가 초기화가 안되어 있으므로 NullPointerException 오류 발생.
		//}
		
		//2. Listdml의 제네릭 타입은 Fruit(클래스명)로 설정되어 있으므로 ..으로 객체를 생성하여 요소를 추가해야 하지만
		//코드에서는 String을 요소로 추가해서 Type MisMatch Error 발생
		//List는 length 라는 메서드가 아닌 size 메서드를 사용한다 Not Found Attribute 
		//index는 배열 객체에서 활용해야 하지만 , list인 곳에 배열 요소 접근 연산인 []이 사용 되었음 Not Allowed Operation Error
		//
		
		//lengtg() 가로 없어야됨 , i+1번째 주민 입력 ,인덱스 7
		//배열은 랭쓰 속성 문자열은 함수 , 
	}
}
